<UserControl
    x:Class="BusinessCredit.TestEngine.UWP.Controls.QuestionControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BusinessCredit.TestEngine.UWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="500"
    d:DesignWidth="1000">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.10*"></RowDefinition>
            <RowDefinition Height="0.70*"></RowDefinition>
            <RowDefinition Height="0.20*"></RowDefinition>
        </Grid.RowDefinitions>
 

        <Viewbox x:Name="viewBoxTitle" Grid.Row="0" Grid.Column="1">
            <TextBlock x:Name="textBlockTitle" 
                       TextWrapping="Wrap" 
                       Text="TextBlock"
                       />
        </Viewbox>

        <Viewbox x:Name="viewBoxContent" Grid.Row="1" Grid.Column="1">

        </Viewbox>

        <Viewbox x:Name="viewBoxAnswers" Grid.Row="2" Grid.Column="1">
            <RelativePanel>
                <StackPanel Orientation="Vertical">

                </StackPanel>
            </RelativePanel>
        </Viewbox>

      
    </Grid>
</UserControl>
